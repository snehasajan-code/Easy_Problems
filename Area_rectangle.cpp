#include<iostream>
using namespace std;
int main()
{
    /*declaring three variables */
    float length,width,area;

    /*input length and width of a rectangle*/
    cout<<"Enter the length: ";
    cin>>length;
    cout<<"Enter the width: ";
    cin>>width;

    /*applying area of rectangle formula */
    area=length*width;

    /*printing the area*/
    cout<<"Area of a rectangle is: "<<area;
    return 0;




}
